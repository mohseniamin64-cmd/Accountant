# واژه‌نامه ساده شبکه و زیرساخت

## دو روش اتصال برای محیط‌های مختلف

- **شبکه داخلی شرکت:** سرور یک IP داخلی پایدار می‌گیرد؛ می‌توان آن را در خود Windows ثابت کرد یا در روتر برای همان دستگاه رزرو کرد. بعد فایروال فقط دسترسی لازم به برنامه را می‌دهد.
- **اتصال مستقیم از بیرون:** معمولاً نیازمند IP عمومی قابل‌دسترسی، تنظیم روتر برای هدایت پورت به سرور و لایه‌های امنیتی مثل HTTPS و فایروال است. بازکردن پورت بدون طرح امنیتی توصیه نمی‌شود.
- **Cloudflare Tunnel:** روشی جایگزین برای انتشار سرویس بدون بازکردن پورت ورودی روتر؛ به تونل، تنظیم دامنه و اجرای `cloudflared` نیاز دارد.

هیچ‌کدام برای همه شرکت‌ها جواب یکسان ندارند؛ روش مناسب را بر اساس شبکه و سیاست امنیتی محل نصب انتخاب می‌کنیم. IP استاتیک روی Windows تنها گزینه نیست؛ رزرو همان IP در روتر (DHCP reservation) هم می‌تواند نشانی داخلی را پایدار نگه دارد.

**وضعیت پروژه:** مالک تأیید کرده اتصال Cloudflare از نوع Tunnel است. نام تونل، دامنه و روش مدیریت تنظیمات هنوز باید روی کامپیوتر بررسی شوند.

این واژه‌نامه اصطلاح‌ها را با مثال همین پروژه توضیح می‌دهد. فعلاً یک کامپیوتر توسعه/آزمایش داریم؛ هنوز سرور عملیاتی شرکت نداریم.

| اصطلاح | معنی ساده | مثال در پروژه |
|---|---|---|
| شبکه داخلی (LAN) | شبکه‌ای که کامپیوترهای داخل شرکت/خانه به آن وصل‌اند؛ لزوماً از اینترنت قابل دسترسی نیست. | حالت فعلی استفاده از برنامه |
| سرور | کامپیوتری که برنامه را اجرا می‌کند و به دستگاه‌های دیگر پاسخ می‌دهد؛ الزاماً دستگاه ویژه‌ای نیست. | کامپیوتر فعلی موقتاً نقش میزبان برنامه را دارد. |
| کلاینت | دستگاه یا مرورگری که به برنامه روی میزبان وصل می‌شود. | کامپیوتر دوم که صفحه برنامه را باز می‌کند. |
| روتر | دستگاهی که شبکه داخلی را به هم وصل می‌کند و معمولاً ارتباط اینترنت را هم مدیریت می‌کند. | مسیریاب شرکت یا مودم/روتر |
| IP | نشانی عددی یک دستگاه در شبکه، مثل نشانی یک خانه. | برای بازکردن برنامه از دستگاه دیگر باید IP میزبان را بدانیم. |
| IP ثابت | نشانی‌ای که برای دستگاه تغییر نمی‌کند یا روتر همیشه همان را به آن می‌دهد. | برای اینکه نشانی میزبان برنامه در LAN عوض نشود. |
| DHCP | قابلیتی در روتر که به دستگاه‌ها خودکار IP می‌دهد. | رزرو DHCP یعنی روتر همیشه یک IP مشخص را به کامپیوتر میزبان بدهد. |
| DNS | دفترچه‌ای که نام قابل‌خواندن را به IP تبدیل می‌کند. | به‌جای به‌خاطر سپردن IP می‌توان از نام استفاده کرد؛ تنظیم پروژه هنوز مستند نشده. |
| پورت (Port) | شماره‌ای برای رسیدن به برنامه/سرویس درست روی یک کامپیوتر؛ مثل شماره واحد. | برنامه وب معمولاً روی 5000؛ PostgreSQL در نصب فعلی روی 5432 و محدود به همان کامپیوتر بود. |
| فایروال | نگهبان شبکه که اجازه یا منع ارتباط را بر اساس برنامه، پورت و نشانی تعیین می‌کند. | فقط ارتباط لازم کاربران به برنامه مجاز باشد؛ دیتابیس را مستقیم برای همه باز نکنیم. |
| دامنه | نام اینترنتی قابل‌خواندن، مثل `example.com`. | دامنه‌ای که شاید برای دسترسی بیرونی استفاده شود؛ نام پروژه هنوز ثبت نشده. |
| Cloudflare | سرویس مدیریت دامنه/DNS و ابزارهای ارتباط امن؛ خودش سرور برنامه یا دیتابیس نیست. | مالک گفته نرم‌افزار/تنظیم Cloudflare روی کامپیوتر فعلی نصب شده؛ نوع اتصال هنوز مشخص نیست. |
| تونل (Tunnel) | مسیر ارتباطی امنی که می‌تواند سرویس داخل شبکه را از راه واسطه به نشانی بیرونی وصل کند. | اگر Cloudflare Tunnel استفاده شده باشد، پس از نصب ویندوز باید عامل و تنظیمات تونل دوباره نصب/بازیابی شوند. |
| TLS/HTTPS | رمزگذاری ارتباط مرورگر و وب‌سایت؛ قفل کنار نشانی مرورگر نشانه رایج آن است. | پیش از دسترسی بیرونی باید HTTPS و تنظیمات امن نشست بررسی شود. |
| دیتابیس | محل منظم نگهداری اطلاعات برنامه. | PostgreSQL اطلاعات حسابداری را نگه می‌دارد. |
| DATABASE_URL | نشانی اتصال برنامه به دیتابیس، معمولاً شامل میزبان، پورت، نام دیتابیس و اعتبارنامه. | چیزی برای دانلود نیست؛ از اطلاعات PostgreSQL ساخته می‌شود و باید محرمانه بماند. |
| Volume در Docker | فضای ذخیره‌سازی جدا از خود کانتینر که داده را پس از تعویض/حذف کانتینر نگه می‌دارد. | volume دیتابیس را باید جدا از کد GitHub پشتیبان گرفت. |
| متغیر محیطی | تنظیمی که برنامه هنگام اجرا از محیط محلی می‌خواند؛ گاهی محرمانه است. | `DATABASE_URL` و `BACKUP_ENCRYPTION_KEY` نمونه‌اند؛ مقدار واقعی را در Git نگذارید. |
| کلید رمزگذاری بکاپ | راز لازم برای بازکردن نسخه پشتیبان رمزگذاری‌شده؛ مثل کلید گاوصندوق. | اگر `BACKUP_ENCRYPTION_KEY` گم شود، بکاپ رمزگذاری‌شده باز نمی‌شود. |
| پشتیبان (Backup) | کپی جداگانه برای برگرداندن داده پس از خرابی یا اشتباه. | بکاپ دیتابیس با کد برنامه فرق دارد؛ هر دو باید جدا حفظ شوند. |

## بعد از نصب دوباره ویندوز

پاک‌شدن برنامه‌ها با نصب ویندوز طبیعی است و به‌تنهایی پروژه را خراب نمی‌کند. نرم‌افزار Cloudflare را دوباره نصب می‌کنیم و اتصال را دوباره برقرار می‌کنیم. اگر تنظیم در حساب Cloudflare مدیریت می‌شود، با دسترسی همان حساب بازسازی/بررسی می‌شود؛ اگر روی کامپیوتر فایل تنظیم یا اعتبارنامه محلی وجود دارد، باید پیش از پاک‌سازی ویندوز در محل امن حفظ شود یا تونل دوباره ساخته شود. هیچ Token یا فایل اعتبارنامه‌ای را در GitHub یا چت قرار ندهید.

نام دامنه، نوع تونل، نام سرویس Cloudflare و نوع فایل تنظیم فعلی هنوز تأیید نشده‌اند؛ برای دستور بازیابی دقیق باید بعداً همین اصطلاح‌ها را روی کامپیوتر بررسی کنیم.
