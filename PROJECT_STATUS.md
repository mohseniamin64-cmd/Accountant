# وضعیت جاری سامانه یکپارچه دیاکو

تاریخ ثبت: 2026-09-25. نقطه ورود: [START_HERE](START_HERE.md).

## وضعیت این مرحله

درخواست جاری کاربر: ساخت بسته ساختاریافته و قابل‌بازیابی در GitHub با تصمیم‌ها، پیاده‌سازی، خطاها، راه‌اندازی و نصب مجدد Windows. اسناد اصلی در ریشه ایجاد شدند. این مرحله مستندسازی است؛ کد محصول، دیتابیس و سرویس‌ها تغییر نکردند.

پروژه در حال توسعه و آزمون تدریجی است. درصد کل پیشرفت تازه تعیین نشده؛ درصدهای قدیمی با دامنه گسترش‌یافته قابل اتکا نیستند. وضعیت روشن بودن پورت و دیتابیس در این مرحله آزموده نشده است.

## وضعیت پشتیبان و بازیابی

- Git origin: `https://github.com/mohseniamin64-cmd/Accountant`, branch `main`; مالک گفته repository فعلاً public است.
- PostgreSQL 16 در Docker volume `diaco-postgres-data` مشاهده شد؛ این داده در GitHub نیست. Docker container روشن بود، ولی listener برنامه روی 3000/4173/5000 نبود.
- پنج `.enc` در `<project-root>\data\backups` دیده شد؛ آخرین تاریخ مشاهده‌شده 1405/06/31. uploads در workspace حاضر خالی است. این‌ها تازگی، داشتن کلید یا restore موفق را اثبات نمی‌کنند.
- کد verify، decrypt موقت و `pg_restore --list` انجام می‌دهد؛ restore واقعی برای database خالی در UI/tool حاضر پیاده نیست و تا آزمون ایزوله، بازیابی تأییدشده نیست.
- در اسناد جدید هیچ مقدار password/key/token ثبت نشده. اسکن regex روی تاریخچه 11 commit صفر تطبیق داشت؛ scanner جامع نصب/اجرا نشده.
- روش شروع ماندگار Node، تعلق/اهمیت container `n8n`, محل امن DB/backup key، مقصد uploads عملیاتی و آزمون restore باید از مالک/متخصص نصب تأیید شود.

## پوشش حوزه‌ها

وجود کد به معنای تأیید همه سناریوها در نصب مشتری نیست. شاهد تاریخی آزمون در [دفتر آزمون‌ها](docs/verification-and-known-issues.md) جداست.

| حوزه | شواهد فعلی / تاریخی | کار باقی‌مانده یا تصمیم آینده |
|---|---|---|
| زیرساخت | React/TypeScript، Node/Express، PostgreSQL؛ فایل‌های migrations تا 016 در مخزن موجود | صحت اجرای migrations و پیکربندی باید در هر نصب بررسی شود؛ ظرفیت عددی تأیید نشده |
| کاربران و نقش‌ها | UserManagementPage و ماژول users؛ کاربر قبلاً ورود، فعال/غیرفعال و خروج نشست را تأیید کرده | حفاظت اختصاصی مالک با مدیر عادی یکی نیست؛ تصمیم DEC-010 نیازمند تطبیق/پیاده‌سازی |
| ورود و امنیت | auth/session-policy، قفل ناموفق و بی‌کاری، تنظیمات امنیت، AuditLogPage | رفع قفل فوری اختصاصی و نمایش همه نشست‌ها نیازمند بررسی؛ تفکیک پایدار آرشیو امنیتی آینده است |
| طرف‌حساب | فرم شخص حقیقی/حقوقی، بایگانی، کد خودکار، جستجوی زنده و نوار جدید در کد | بررسی فیلتر روی کل داده‌ها و یکتایی؛ شهرها فهرست گزیده دارند |
| کالا و سازمان | محصولات، شعبه و انبار؛ حذف کالای بدون سابقه و کد خودکار در سابقه آزمون کاربر | آزمون کامل داده وابسته و عدم حذف اقلام دارای گردش |
| خرید و فروش | trade/routes، invoice.service و return.service؛ پیش‌نویس/قطعی و مرجوعی | بازطراحی شبیه هلو متوقف‌شده در گفتگو؛ پیش‌فاکتور، امانی دوطرفه، ضایعات و بازپرداخت مستقل تکمیل/تطبیق شوند |
| انبار | inventory.service، گردش و ارتباط با خرید/فروش/تولید | تست رقابت هم‌زمان و صحت موجودی روی داده زیاد |
| تولید | BOM، دستور تولید، رزرو و سریال | اصلاح پیشرفته، کسری/ضایعات و آزمون واقعی چندمرحله‌ای |
| خدمات و گارانتی | صفحه مستقل پذیرش، صف، قطعات، آزمون نهایی، تحویل، چاپ و رهگیری | آزمون کامل پرونده واقعی، پرینتر، گوشی و پیامک فیزیکی |
| خزانه و حسابداری | تراکنش، تخصیص، چک، اسناد و گزارش‌های مالی در کد | بازپرداخت مستقل، مالیات/صورتحساب رسمی و مودیان نیازمند برنامه جدا |
| سال مالی | عملیات سال مالی در ماژول حسابداری وجود دارد | آرشیو فشرده کامل، انتقال انتخابی به DB جدید و جستجوی چندساله تصمیم آینده‌اند |
| بکاپ و تنظیمات | backup service/scheduler، لوگو، چاپ، درگاه پیامک؛ بکاپ داخلی و اعتبارسنجی در گفتگو تأیید شده | اعتبارسنجی ساختار معادل بازیابی کامل نیست؛ بکاپ خارجی و اتصال واقعی پیامک به تعویق افتاده |
| راهنما | راهنمای صفحات و فایل‌های فارسی؛ راهنمای جامع Markdown در این مرحله | PDF تصویردار و مرور کاربری تمام صفحات هنوز تحویل نهایی نشده |
| آزمایش ظرفیت | طرح تأییدشده ابزار آفلاین مولد داده و بار شبکه | ابزار و تست ظرفیت در این مرحله ساخته/اجرا نشده |

## یافته‌های بازبینی مستندات

- README قدیمی پورت ۴۱۷۳ داشت؛ مرجع اجرای فعلی و شواهد در سند معماری است. پورت هدف پروژه ۵۰۰۰ است.
- قاعده قدیمی رمز مالک، تغییر رمز توسط خودش را منع می‌کرد؛ اصلاح نهایی اجازه تغییر با رمز فعلی و بازیابی فراموشی فقط روی سرور می‌دهد.
- منع مطلق هم‌نامی طرف‌حساب با تصمیم شرطی حقیقی/حقوقی جایگزین شده است؛ جزئیات DEC-006.
- کد PartiesPage فیلتر نوع شخص و همکاری را بعد از دریافت صفحه نتایج اعمال می‌کند؛ نیازمند بررسی نتایج و صفحه‌بندی در داده زیاد است.
- جستجوی SQL نام از ILIKE با wildcard دوطرفه استفاده می‌کند؛ نمونه گفتگوی کاربر درباره شروع نام با «م» نباید به‌عنوان تست تطبیق پیشوندی گزارش شود.
- سیاست سه‌سطحی مدل Luna/Terra/Sol در سند قدیمی با سیاست فقط Luna جایگزین شده است.

## ادامه کار

1. ابتدا پاسخ موارد باز در [WINDOWS-REINSTALL](WINDOWS-REINSTALL.md) و [RUNBOOK](RUNBOOK.md) را تکمیل، ابزار restore امن را بسازید/تست کنید، سپس بسته را commit/push و در GitHub بازبینی کنید.
2. از [رودمپ](ROADMAP.md) و [تحویل](docs/project-governance/handoff.md) استفاده شود.
3. کاربری فعلی: مرور طرف‌حساب‌ها و کاربران؛ فاکتورها تا تصمیم کاربر درباره ادامه در حالت تعویق باقی بمانند.
4. ابزار ارزیابی ظرفیت دارای طرح است؛ «روز بعد» در گفتگو برنامه‌ریزی بوده و به معنای اجرای خودکار/زمان‌بندی قطعی نیست.

## سوابق

تمام دفتر وضعیت پیشین در [آرشیو](docs/archive/project-status-before-2026-09-25.md) حفظ شده است. گزارش‌های قدیمی ۸۲، ۱۰۹، ۱۱۲ و ۱۱۴ تست مربوط به زمان خود هستند؛ در این مرحله آزمون کد دوباره اجرا نشده و هیچ‌کدام نتیجه تازه محسوب نمی‌شوند.

## الحاقیه راستی‌آزمایی این بسته — 2026-09-25

پس از ایجاد مستندات بازیابی:

- npm run lint: موفق، tsc بدون خطا.
- npm test: موفق؛ 26 فایل آزمون و 114 تست.
- npm run build: موفق؛ خروجی Vite و dist/server.cjs ساخته شد.
- هشدار build: چند chunk از 500 kB بزرگ‌تر است؛ خطای blocking نیست و code-splitting می‌تواند کار آینده باشد.
- health، migration روی دیتابیس زنده، اتصال سرویس خارجی و restore کامل backup در این مرحله اجرا نشده‌اند.
