# تحویل جاری برای توسعه‌دهنده یا مدل بعدی

تاریخ: 2026-09-25. ابتدا [START_HERE](../../START_HERE.md) و سپس [وضعیت](../../PROJECT_STATUS.md) خوانده شود.

## کار انجام‌شده در این مرحله

سازمان‌دهی مستندات برای کل محصول، حفظ دفتر قدیمی در آرشیو، ثبت ۲۶ تصمیم با اصلاحیه‌ها، جداسازی وضعیت اجرا از پیشنهاد، رودمپ، قواعد، راهنمای همه ماژول‌ها و طرح ابزار آفلاین ظرفیت. این مرحله هیچ قابلیت محصولی را پیاده‌سازی نکرد.

در درخواست جدید، بسته بازیابی root-level شامل README, ROADMAP, ARCHITECTURE, DECISIONS, IMPLEMENTATION, TROUBLESHOOTING, RUNBOOK, WINDOWS-REINSTALL, SECURITY-NEXT-STEPS, DATABASE, DEPLOYMENT, BACKUP-RESTORE, ENVIRONMENT و API افزوده شد. draft تا روشن‌شدن روش اجرای app، منبع امن کلید/DB، و تعلق n8n باز است؛ بازیابی dump در برنامه هنوز پیاده/آزموده نیست. GitHub طبق اعلام مالک public است. مقصد push، commit و بازخوانی نهایی پس از رفع موارد باز ثبت شود.

## زمینه حفظ‌شده

بازبینی مستندات همین مرحله: ۱۷ فایل جاری و ۱۶۴ پیوند محلی بررسی شد؛ پیوند مفقود صفر بود. بررسی whitespace تغییرات Markdown ردیابی‌شده خطایی نداشت؛ فقط هشدار تبدیل LF/CRLF محیط ویندوز دیده شد. آرشیو تاریخی در شمارش پیوندهای جاری نیست. تست برنامه، build، health، migration و اتصال دیتابیس اجرا نشدند.

- کاربر ظاهر طرف‌حساب و کاربران را تدریجی بررسی کرده و استاندارد آن را برای بقیه بخش‌ها می‌خواهد.
- آخرین ترتیب نوار طرف‌حساب: ایجاد، وضعیت، نوع شخص، جستجو، فیلترهای بیشتر؛ جستجو زنده و بی‌دکمه.
- رنگ زمینه عملیات کاربر نهایی شده: سبز/کهربایی/قرمز/آبی/بنفش طبق قواعد.
- کاربر بهبود چرخه بررسی→اصلاح→تست را خواسته؛ شکست عادی نباید با درخواست حل فنی از کاربر خاتمه یابد.
- فقط Luna برای واگذاری؛ اگر کاربر Task مستقل می‌خواهد، آن را به‌عنوان subagent معرفی نکنید.
- فاکتورها برای تمرکز بر کاربران/طرف‌حساب عقب افتاده‌اند؛ سال مالی و ابزار ظرفیت در سطح تصمیم‌اند.

## محل کدهای مرتبط

| موضوع | فایل/پوشه |
|---|---|
| اطلاعات پایه و UI مرجع | src/app/PartiesPage.tsx، MasterDataUi.tsx، parties-page.css، server/modules/parties |
| کاربران و امنیت | src/app/UserManagementPage.tsx، user-management.css، server/modules/users، server/modules/auth |
| راه‌اندازی و pool | scripts/start-production.mjs، server/config.ts، server/db/pool.ts |
| راهنمای صفحات | src/app/help-content.ts، master-data.help.ts و راهنماهای docs |
| ماژول‌ها و routeها | server/app.ts، server/modules، src/app/App.tsx |
| schema | server/db/migrations؛ وجود فایل با اجرا روی DB برابر نیست |

## مواردی که نباید اشتباه منتقل شوند

- حفاظت مالک مستند شده؛ administrator معمولی لزوماً مالک یکتا نیست. اصلاح رمز مالک طبق DEC-010 خوانده شود.
- ۲۰ اتصال pool به معنی ۲۰ کاربر نیست؛ هیچ آزمون ظرفیت ۵۰/۱۰۰ کاربر در این مرحله اجرا نشده.
- گزارش قدیمی ۱۱۴ تست و PID/health تاریخی، وضعیت امروز را نشان نمی‌دهد.
- فایل‌های واقعی env و داده/بکاپ/اسرار نباید وارد گزارش یا Git شوند.
- حذف مشتری با غیرفعال/بایگانی و لاگ امنیتی با اسناد مالی متفاوت‌اند.

## وضعیت محیط و احتیاط ادامه

در نوبت مستندسازی پیشین، worktree تغییرات tracked و untracked داشت و دست‌نخورده نگه داشته شد. در ابتدای این نوبت بسته‌بندی مستندات، شاخه `main` با HEAD=`948024f` و درخت کاری تمیز بود. پورت ۵۰۰۰ هدف است، اما سلامت برنامه در نوبت جاری آزمایش نشده است.

بازبینی جدید در همان workspace نشان داد Docker `diaco-postgres` (`postgres:16-alpine`) با named volume `diaco-postgres-data` روشن بود؛ app listener روی 3000/4173/5000 نبود؛ پنج backup رمزگذاری‌شده در `data/backups` تا تاریخ 1405/06/31 دیده شد؛ `data/uploads` در همین workspace خالی بود. این snapshot به معنای صحت/restore یا وضعیت نصب دیگری نیست.

بسته root docs ابتدا در commit `3625eb3563714a5dbd20fd818b69106a9df32320` به `main` رفت و remote دوباره خوانده شد؛ 14 فایل الزامی موجود بود، 192 پیوند محلی بی‌نقص و الگوی Secret در Markdown برابر صفر بود. پس از push، کامل‌بودن دفتر تصمیم و خطاهای transcript مرور شد و همین تغییرات corrective هنوز باید push شوند. در workspace اجرای 2026-09-25: `npm run lint`, `npm test` (26/114) و `npm run build` موفق؛ health/migration/restore کامل اجرا نشده.

برای restart آینده قبل از توقف فرایند، روش اجرا و دسترسی به تنظیمات معتبر را بررسی کنید. سابقه شکست ناشی از نبود DATABASE_URL در محیط جدید وجود دارد؛ از نمونه یا حدس رمز استفاده نشود.

## قدم بعدی

با درخواست جدید کاربر حوزه مرحله اجرایی مشخص شود. اگر ادامه همین حوزه خواسته شد، موارد R1/R2 و یافته‌های دفتر آزمون را بررسی کنید. اگر آزمایش ظرفیت خواسته شد، ابتدا محیط مجزا و سناریوی طرح capacity-lab را آماده کنید. این سند به‌تنهایی مجوز ساخت همه آینده‌ها یا شروع فشار روی سرور واقعی نیست.

## تکمیل handoff بعدی

در پایان هر مرحله بنویسید: درخواست دقیق، تصمیم‌های جدید/جایگزین، فایل‌های تغییرکرده، migration اجراشده یا نشده، آزمون واقعی با تاریخ/محیط، موارد باز، وضعیت عملیاتی مشاهده‌شده و اولین اقدام بعدی. اگر چیزی بررسی نشده همان را صریح بنویسید.

## راستی‌آزمایی نهایی بسته — 2026-09-25

در این نوبت npm run lint موفق شد، npm test با ۲۶ فایل و ۱۱۴ تست موفق شد، و npm run build موفق شد. health زنده، migration روی دیتابیس موجود و restore کامل backup همچنان انجام نشده‌اند.
