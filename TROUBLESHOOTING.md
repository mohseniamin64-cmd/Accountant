# خطاها و راه‌حل‌ها

## مواردی که در گفتگو دیده شدند

این‌ها تجربه‌های کاربر در نسخه/محیط همان زمان‌اند، نه نتیجه regression فعلی.

| علامت دقیق | علت یا شواهد | اقدام/نتیجه | وضعیت مستند |
|---|---|---|---|
| `can't access property "reset", T.currentTarget is null` هنگام ساخت کاربر با نقش admin | خطای frontend؛ دسترسی `currentTarget` بعد از async محتمل، علت از log/stack قابل اثبات نیست | کاربر بعداً صفحه کاربران را دید و اصلاحات UI را تأیید کرد | patch و تست دقیق این مورد مشخص نیست |
| ورود هیچ کاری نمی‌کند/spinner می‌ماند | علت دقیق ثبت نشده؛ احتمال خطای handler/API، نه خطای validation لزوماً | پیام «نام کاربری الزامی است» و «رمز عبور الزامی است» دیده شد؛ بعداً ورود موفق گزارش شد | علت ریشه‌ای نامعلوم |
| بعد از بستن browser صفحه ورود می‌آید | نشست browser/session expiry؛ کاربر گفت یک دقیقه بعد منوها باز می‌شوند اما save کار نمی‌کند | lock خودکار تنظیم و کاربر اعمال روی نشست فعال را تأیید کرد | سناریوی فعلی نیازمند regression |
| پشتیبان: `spawn pg_dump ENOENT` | executable در PATH نیست | بعداً خروجی backup و `restore_verification` موفق گزارش شد | مشخص نیست pg_dump نصب شد یا Docker fallback فعال شد |
| کلید BACKUP_ENCRYPTION_KEY لازم است | missing یا Base64 decode کمتر/بیشتر از 32 بایت | کاربر کلید را ساخت، backup settings ذخیره شد | مقدار فقط از محل امن گرفته شود |
| cancel غیرفعال‌سازی پیام «دلیل وارد نشده» داد | دکمه cancel در validation دلیل افتاده بود | اصلاح cancel بعداً تأیید شد | confirm همچنان مسئله جدا بود |
| confirm طرف‌حساب: `PATCH /users/<id>/status` و «مسیر پیدا نشد» | screen request به users route می‌رفت در حالی که entity طرف‌حساب بود؛ در screenshot شاهد 404 endpoint | cancel درست شد ولی user گفت تأیید ثبت نمی‌شود | رفع نهایی این route در conversation اثبات نشده؛ حتماً endpoint / parties و enable/disable را تست کنید |
| طرف‌حساب بعد از غیرفعال‌سازی از فهرست محو شد | فیلتر پیش‌فرض Active یا path خطا؛ archive و inactive باید جدا باشند | فیلترهای همه/فعال/غیرفعال/بایگانی طراحی شدند؛ بعداً کاربر گفت filter صحیح است | فعال‌سازی مجدد/سوابق را با داده آزمایشی تایید کنید |
| online/offline با وضعیت فعال حساب قاطی شده بود | account status و live session مستقل‌اند | کاربر هر سه online/offline و logout یک نشست را مشاهده کرد | این گزارش تاریخی است؛ هر دستگاه/expiry تازه نیازمند تست است |
| متن تعداد تلاش‌ها «موفق» به جای «ناموفق» | احتمال اشتباه label در متن قفل | کاربر گفت خود پیام lock درست است و خواست fix قبلی اجرا نشود | دستور کاربر: آن تغییر را انجام ندهید مگر دوباره بخواهد |
| هم‌نامی طرف‌حساب و تغییر اکبر به علی، دو علی ساخت | یکتایی نیازمند check سمت server و قاعده entity-specific است | قاعده تصمیم شد، migrationهای name/identity وجود دارند | SQL مشکوک؛ قبل از اصلاح، موارد هم‌نام/شناسه تفاوت/خالی را test کنید |
| جستجوی prefix به substring تبدیل شده/فیلتر نتایج کم است | SQL `ILIKE '%query%'`; فیلتر client پس از صفحه 30 | در code review ثبت شد | search behavior و pagination server-side باز |
| همه شهرها در فرم پیدا نمی‌شوند | داده استان/شهر فهرست گزیده است | منبع محدود `provinceCities` شناسایی شد | داده جامع و keyboard navigation باز |
| آدرس نمونه 4173 اما درخواست پورت 5000 | نمونه `.env.example`, development default و production script فرق دارند | production 5000 مستند شد | نمونه config باید جداگانه یکدست شود |
| تغییر نام Windows و اتصال DB | user در نرم‌افزار قبلی DB را با نام کامپیوتر از دست داده بود | این برنامه از PostgreSQL و URL اتصال استفاده می‌کند؛ در محیط بازبینی DB روی loopback بود | hostname/IP/service/volume config واقعی هنوز باید بعد از rename سنجیده شود |
| دیتابیس/سرور پس از بازکردن terminal نیست | `DATABASE_URL` برای process جدید وجود نداشت یا پایدار نبود | ساخت `.env` با مقادیر امن مستند شد | محل واقعی config در Windows نامعلوم |
| فرم فاکتور نامنظم/بسیار عریض و alignmentها | بازخورد بصری متوالی | بعضی صفحه‌ها اصلاح و تأیید شدند؛ کل UI یکجا تأیید نشده | ادامه UX طبق roadmap |
| پورت مورد انتظار بالا نبود | در snapshot 2026-09-25 app ports listener نداشت | app توسط این مستندسازی start نشد | اگر کاربر انتظار اجرای زنده دارد، Runbook و env نصب را بررسی کن |

## نبودن DATABASE_URL
- علامت: config هنگام start به اتصال دیتابیس نیاز دارد و برنامه بالا نمی‌آید.
- علت: environment معتبر تنظیم نشده است.
- تست: بررسی env محلی بدون نمایش مقدار.
- راه‌حل: DATABASE_URL را از محل امن یا Dashboard مربوطه دریافت و فقط در environment محلی تنظیم کنید.

## نبودن یا نامعتبر بودن کلید backup
- علامت: BACKUP_KEY_NOT_CONFIGURED یا INVALID_BACKUP_KEY.
- علت: کلید تنظیم نشده یا قالب/طول آن مطابق انتظار نیست.
- راه‌حل: این Credential باید از محل امن یا Dashboard مربوطه دوباره دریافت شود؛ مقدار واقعی ثبت نمی‌شود.

## اختلاف پورت
- علامت: یک سند 4173 و اسکریپت production عدد 5000 را انتظار دارد.
- علت: .env.example برای توسعه و start-production.mjs دو مسیر متفاوت دارند.
- راه‌حل: برای production اسکریپت start و PORT واقعی را مرجع بگیرید و reverse proxy/firewall را با همان مقدار تنظیم کنید.

## migration checksum mismatch
- علامت: migration به‌دلیل checksum متفاوت متوقف می‌شود.
- علت: فایل migration اعمال‌شده تغییر کرده است.
- راه‌حل: فایل قبلی را بازنویسی نکنید؛ diff را بررسی و migration جدید بسازید. قبل از هر اصلاح backup بگیرید.

## backup پیدا نمی‌شود یا hash mismatch
- علامت: BACKUP_NOT_FOUND یا BACKUP_HASH_MISMATCH.
- علت: مسیر اشتباه، فایل ناقص یا دستکاری‌شده.
- راه‌حل: مقصد، اندازه، hash و دسترسی را بررسی کنید؛ فایل مشکوک را restore نکنید.

## آزمون‌نشده‌بودن سخت‌افزار
- علامت: بارکدخوان، چاپگر یا SMS در کد هست اما نتیجه میدانی نداریم.
- راه‌حل: تست کنترل‌شده انجام و نتیجه، مدل دستگاه، درایور و خطا ثبت شود.

## خطای ویرایش مستندات روی مسیر فارسی
- علامت: ابزار ویرایش داخلی به‌دلیل ACL نتوانست فایل را بخواند.
- علت: محدودیت دسترسی محیط اجرا، نه منطق برنامه.
- راه‌حل: نوشتن کنترل‌شده محلی و سپس git diff/check انجام شد.
